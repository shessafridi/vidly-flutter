import 'package:flutter/material.dart';

class MovieFormPage extends StatelessWidget {
  final String id;
  const MovieFormPage({required this.id, Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
